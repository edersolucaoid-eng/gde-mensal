---
import Base from '../../layouts/Base.astro';
---
<Base title="Desafío Portero de Elite | 5 Días de Entrenamiento Físico Específico para Porteros" heroImage="/images/logo-gde.webp">
<Fragment slot="head">
  <script is:inline>!function(i,n){i._plt=i._plt||(n&&n.timeOrigin?n.timeOrigin+n.now():Date.now())}(window,performance);</script>
  <link rel="preload" href="https://scripts.converteai.net/54614f94-0915-47ec-b3bb-c6614fdad9d2/players/6a18a8b89450495a8b528dfa/v4/player.js" as="script" />
  <link rel="preload" href="https://scripts.converteai.net/lib/js/smartplayer-wc/v4/smartplayer.js" as="script" />
  <link rel="dns-prefetch" href="https://cdn.converteai.net" />
  <link rel="dns-prefetch" href="https://scripts.converteai.net" />
  <link rel="dns-prefetch" href="https://images.converteai.net" />
  <link rel="dns-prefetch" href="https://license.vturb.com" />
</Fragment>
<style>
/* ─── Reset & base ─────────────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'Barlow', sans-serif;
  background: #0B0B0B;
  color: #fff;
  overflow-x: hidden;
}

/* ─── Tokens ────────────────────────────────────────────────── */
:root {
  --black:  #0B0B0B;
  --white:  #ffffff;
  --yellow: #F4B400;
  --sec-v: clamp(60px, 8vw, 100px);
  --wrap: 860px;
  --wrap-wide: 1040px;
}

/* ─── Layout ────────────────────────────────────────────────── */
.wrap      { max-width: var(--wrap);      margin: 0 auto; padding: 0 clamp(24px,5vw,48px); }
.wrap-wide { max-width: var(--wrap-wide); margin: 0 auto; padding: 0 clamp(24px,5vw,48px); }

.s-dark  { background: var(--black); color: var(--white); padding: var(--sec-v) 0; }
.s-light { background: var(--white); color: var(--black); padding: var(--sec-v) 0; }
.s-yellow{ background: var(--yellow);color: var(--black); padding: var(--sec-v) 0; }

/* ─── Section label ─────────────────────────────────────────── */
.label {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .65rem;
  font-weight: 700;
  letter-spacing: .22em;
  text-transform: uppercase;
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 18px;
}
.label::before {
  content: '';
  display: block;
  width: 24px;
  height: 1px;
}
.s-dark  .label { color: var(--yellow); }
.s-dark  .label::before { background: var(--yellow); }
.s-light .label { color: rgba(0,0,0,.4); }
.s-light .label::before { background: rgba(0,0,0,.4); }
.s-yellow .label { color: rgba(0,0,0,.55); }
.s-yellow .label::before { background: rgba(0,0,0,.55); }

/* ─── Headings ──────────────────────────────────────────────── */
.display {
  font-family: 'Bebas Neue', sans-serif;
  line-height: .88;
  letter-spacing: .01em;
  font-size: clamp(3.2rem, 9vw, 6.5rem);
  margin-bottom: clamp(48px, 7vw, 80px);
}
.display .yl   { color: var(--yellow); }
.display .faded{ color: rgba(255,255,255,.18); }

hr {
  border: none;
  height: 1px;
  margin-bottom: clamp(48px, 7vw, 80px);
}
.s-dark  hr { background: rgba(255,255,255,.08); }
.s-light hr { background: rgba(0,0,0,.1); }

/* ─── LOCKED STATE (página cerrada hasta 2:40 del VSL) ────── */
body.is-locked .schedule-sec,
body.is-locked .s-dark,
body.is-locked .s-light,
body.is-locked .plan-sec,
body.is-locked .foot { display: none !important; }
.hero-cta.is-hidden { display: none !important; }
.hero-vsl {
  width: 100%;
  max-width: 420px;
  margin: clamp(8px,2vw,18px) auto 0;
}
.hero-cta {
  position: relative;
  overflow: hidden;
  align-self: center;
  margin-top: clamp(20px,3vw,32px);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 18px 44px;
  border-radius: 14px;
  background: linear-gradient(180deg, #F4C400 0%, #E0A800 100%);
  box-shadow: 0 16px 40px rgba(244,180,0,.32);
  font-family: 'Barlow Condensed', sans-serif;
  font-size: 1.05rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--black);
  text-decoration: none;
  transition: opacity .15s, transform .2s, box-shadow .3s;
  animation: ctaPulse 2.6s ease-in-out infinite;
}
.hero-cta:hover {
  opacity: .95;
  transform: translateY(-3px);
  box-shadow: 0 24px 60px rgba(244,180,0,.55);
  animation-play-state: paused;
}
.hero-cta::after {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(120deg, transparent 30%, rgba(255,255,255,.55) 50%, transparent 70%);
  transform: translateX(-100%);
  transition: transform .7s ease;
  pointer-events: none;
}
.hero-cta:hover::after { transform: translateX(100%); }

/* ─── HERO CENTRALIZADO (VSL) ──────────────────────────────── */
.hero {
  background: var(--black);
  min-height: 100svh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: clamp(20px,4vw,48px) clamp(16px,4vw,32px);
}
.hero-inner {
  width: 100%;
  max-width: 720px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: clamp(28px,4vw,40px);
}
.hero-text {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 20px;
}
.hero-eyebrow {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .7rem;
  font-weight: 700;
  letter-spacing: .24em;
  text-transform: uppercase;
  color: var(--yellow);
}
.hero-headline {
  font-family: 'Bebas Neue', sans-serif;
  font-size: clamp(1.5rem, 3.2vw, 2.2rem);
  line-height: 1.05;
  color: var(--white);
  letter-spacing: .01em;
  margin: 0;
  max-width: 620px;
}
.hero-headline .yl { color: var(--yellow); }
.hero-media {
  order: -1;
  width: 100%;
  max-width: 420px;
}
.hero-media img {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 16px;
  margin: 0 auto;
}
@media (max-width: 900px) {
  .hero { padding: 0 0 clamp(32px,7vw,52px); min-height: auto; }
  .hero-inner { gap: 24px; }
  .hero-media { max-width: 100%; }
  .hero-media img {
    max-width: 100%;
    border-radius: 0;
    margin: 0;
    width: 100%;
  }
  .hero-text {
    padding: 0 24px;
  }
}

/* ─── CRONOGRAMA DA SEMANA ─────────────────────────────────── */
.schedule-sec {
  background: var(--black);
  padding: clamp(40px,6vw,72px) 0;
  border-bottom: 1px solid rgba(255,255,255,.04);
}
.schedule-intro {
  text-align: center;
  max-width: 540px;
  margin: 0 auto clamp(28px,4vw,40px);
  padding: 0 clamp(24px,5vw,48px);
}
.schedule-intro .label { justify-content: center; margin-bottom: 14px; }
.schedule-intro h3 {
  font-family: 'Bebas Neue', sans-serif;
  font-size: clamp(1.6rem,3vw,2.2rem);
  letter-spacing: .02em;
  color: var(--white);
  line-height: 1.05;
}
.schedule-grid {
  max-width: var(--wrap-wide);
  margin: 0 auto;
  padding: 0 clamp(24px,5vw,48px);
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12px;
}
.schedule-card {
  background: rgba(255,255,255,.04);
  border: 1px solid rgba(255,255,255,.08);
  border-radius: 12px;
  padding: 22px 18px 24px;
  display: flex;
  flex-direction: column;
  transition: transform .35s ease, border-color .3s, background .3s, box-shadow .3s;
}
.schedule-card:hover {
  transform: translateY(-4px);
  border-color: rgba(244,180,0,.45);
  background: rgba(244,180,0,.06);
  box-shadow: 0 18px 44px rgba(244,180,0,.22);
}
.schedule-n {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 1.6rem;
  color: var(--yellow);
  line-height: 1;
  letter-spacing: .04em;
  margin-bottom: 14px;
}
.schedule-day {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .68rem;
  font-weight: 700;
  letter-spacing: .22em;
  text-transform: uppercase;
  color: rgba(255,255,255,.5);
  margin-bottom: 6px;
}
.schedule-title {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: 1.02rem;
  font-weight: 700;
  letter-spacing: .04em;
  text-transform: uppercase;
  color: var(--white);
  line-height: 1.15;
  margin-bottom: 12px;
}
.schedule-desc {
  font-size: .78rem;
  color: rgba(255,255,255,.42);
  line-height: 1.5;
}
@media (max-width: 900px) {
  .schedule-grid { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 600px) {
  .schedule-grid { grid-template-columns: 1fr; }
}

/* ─── COUNTDOWN ─────────────────────────────────────────────── */
.cd-block {
  text-align: center;
  max-width: 520px;
  margin: 0 auto;
}
.cd-block .label { justify-content: center; margin-bottom: 4px; }
.cd-time {
  font-family: 'Bebas Neue', sans-serif;
  font-size: clamp(3rem, 9vw, 5rem);
  letter-spacing: .04em;
  color: var(--white);
  line-height: 1;
  margin: 12px 0 10px;
}
.cd-labels {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .58rem;
  font-weight: 700;
  letter-spacing: .28em;
  text-transform: uppercase;
  color: rgba(255,255,255,.3);
  margin-bottom: 18px;
}
.cd-date {
  font-size: .82rem;
  color: rgba(255,255,255,.55);
  margin-bottom: 28px;
  font-weight: 500;
}
.cd-date span { color: var(--white); }
.cd-bar {
  height: 2px;
  background: rgba(255,255,255,.08);
  border-radius: 1px;
  overflow: hidden;
  margin-bottom: 10px;
}
.cd-fill {
  height: 100%;
  background: var(--yellow);
  width: 74.3%;
}
.cd-slots {
  font-size: .74rem;
  color: rgba(255,255,255,.4);
  letter-spacing: .03em;
  font-weight: 500;
}
.cd-slots strong { color: var(--yellow); font-weight: 700; }
.cd-cta {
  margin-top: 28px;
  font-size: 1.05rem;
  padding: 18px 36px;
  border-radius: 14px;
  animation: ctaPulse 2.6s ease-in-out infinite;
}
.cd-cta:hover { animation-play-state: paused; }
.cd-foot {
  margin-top: 14px;
  font-size: .72rem;
  letter-spacing: .04em;
  color: rgba(255,255,255,.4);
}

/* ─── PROBLEM LIST ──────────────────────────────────────────── */
.pain-list {
  list-style: none;
  border-top: 1px solid rgba(0,0,0,.1);
}
.pain-item {
  display: flex;
  align-items: center;
  gap: 20px;
  padding: 22px 0;
  border-bottom: 1px solid rgba(0,0,0,.1);
}
.pain-x {
  width: 26px;
  height: 26px;
  border: 1.5px solid var(--black);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: .62rem;
  font-weight: 700;
  color: var(--black);
  flex-shrink: 0;
}
.pain-item span {
  font-size: 1rem;
  font-weight: 500;
  color: var(--black);
  line-height: 1.3;
}
.pivot-quote {
  margin-top: 40px;
  padding: 28px 32px;
  background: var(--black);
  font-size: clamp(.95rem,2.2vw,1.08rem);
  font-weight: 500;
  color: rgba(255,255,255,.45);
  line-height: 1.7;
  border-left: 3px solid var(--yellow);
}
.pivot-quote strong { color: var(--yellow); }

/* ─── PROOF CAROUSEL ─────────────────────────────────────────── */
.proof-clip {
  overflow: hidden;
  width: 100%;
  margin-top: clamp(40px,6vw,64px);
}
.proof-carousel-outer {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  perspective: 1200px;
}
.proof-track {
  position: relative;
  width: 100%;
  height: 460px;
  display: flex;
  align-items: center;
  justify-content: center;
  transform-style: preserve-3d;
}
.proof-slide {
  position: absolute;
  width: 260px;
  height: 400px;
  border-radius: 20px;
  overflow: hidden;
  border: 1px solid rgba(255,255,255,.1);
  transition: transform .5s ease, opacity .5s ease, filter .5s ease, box-shadow .5s ease, border-color .5s ease;
  cursor: pointer;
}
.proof-slide img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}
.proof-slide-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(0,0,0,.88) 0%, rgba(0,0,0,.2) 55%, transparent 100%);
}
.proof-caption {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 18px 18px 22px;
  z-index: 2;
}
.proof-role-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  border-radius: 9999px;
  background: rgba(255,255,255,.08);
  border: 1px solid rgba(255,255,255,.15);
  padding: 3px 10px;
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .62rem;
  letter-spacing: .08em;
  color: rgba(255,255,255,.75);
  margin-bottom: 7px;
}
.proof-slide-name {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: 1.25rem;
  font-weight: 700;
  letter-spacing: .03em;
  color: var(--white);
  line-height: 1.1;
}
.proof-active-badge {
  display: none;
  position: absolute;
  top: 14px;
  right: 14px;
  z-index: 3;
  background: var(--yellow);
  color: var(--black);
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .6rem;
  font-weight: 700;
  letter-spacing: .1em;
  text-transform: uppercase;
  border-radius: 9999px;
  padding: 4px 10px;
}
.proof-slide.is-active { border-color: rgba(244,180,0,.5); }
.proof-slide.is-active .proof-active-badge { display: block; }
.proof-nav {
  position: absolute;
  z-index: 20;
  width: 44px;
  height: 44px;
  border-radius: 50%;
  background: rgba(0,0,0,.55);
  border: 1px solid rgba(255,255,255,.1);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  color: var(--white);
  transition: background .2s;
  flex-shrink: 0;
}
.proof-nav:hover { background: rgba(255,255,255,.08); }
.proof-nav-prev { left: 0; }
.proof-nav-next { right: 0; }

/* ─── METHOD LIST ───────────────────────────────────────────── */
.method-list { border-top: 1px solid rgba(0,0,0,.1); }
.method-item {
  display: grid;
  grid-template-columns: 80px 1fr;
  border-bottom: 1px solid rgba(0,0,0,.1);
  padding: 36px 0;
  gap: 0;
}
.method-n {
  font-family: 'Bebas Neue', sans-serif;
  font-size: clamp(3rem,7vw,4.5rem);
  color: var(--yellow);
  line-height: 1;
  padding-right: 24px;
  border-right: 1px solid rgba(0,0,0,.1);
  align-self: start;
}
.method-body { padding-left: 32px; padding-top: 4px; }
.method-title {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: 1.1rem;
  font-weight: 700;
  letter-spacing: .05em;
  text-transform: uppercase;
  color: var(--black);
  margin-bottom: 12px;
}
.method-ul { list-style: none; display: flex; flex-direction: column; gap: 8px; }
.method-ul li {
  font-size: .88rem;
  color: rgba(0,0,0,.55);
  display: flex;
  gap: 12px;
  align-items: center;
}
.method-ul li::before {
  content: '';
  width: 16px;
  height: 1px;
  background: var(--yellow);
  flex-shrink: 0;
}

/* ─── METHOD RESULT ────────────────────────────────────────── */
.method-result {
  display: flex;
  gap: 10px;
  margin-top: 16px;
  padding-top: 14px;
  border-top: 1px solid rgba(244,180,0,.22);
  font-size: .92rem;
  color: var(--black);
  font-weight: 600;
  line-height: 1.45;
}
.method-result::before {
  content: '→';
  color: var(--yellow);
  font-weight: 700;
  flex-shrink: 0;
}
.method-bonus {
  margin-top: 28px;
  padding: 18px 22px;
  background: rgba(244,180,0,.08);
  border-left: 3px solid var(--yellow);
  font-size: .9rem;
  color: rgba(0,0,0,.72);
  line-height: 1.55;
}
.method-bonus strong { color: var(--black); }

/* ─── BOTÃO CTA INLINE ─────────────────────────────────────── */
.btn-inline-wrap { text-align: center; margin-top: 36px; }
.btn-inline {
  position: relative;
  overflow: hidden;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 16px 36px;
  border-radius: 12px;
  background: linear-gradient(180deg, #F4C400 0%, #E0A800 100%);
  box-shadow: 0 10px 32px rgba(244,180,0,.22);
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .95rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--black);
  text-decoration: none;
  transition: opacity .15s, transform .2s, box-shadow .3s;
}
.btn-inline:hover {
  opacity: .95;
  transform: translateY(-2px);
  box-shadow: 0 18px 44px rgba(244,180,0,.4);
}

/* ─── INCLUDED TABLE ────────────────────────────────────────── */
.inc-table { border-top: 1px solid rgba(255,255,255,.06); }
.inc-row {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 22px 0;
  border-bottom: 1px solid rgba(255,255,255,.06);
  gap: 24px;
}
.inc-name {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .95rem;
  font-weight: 700;
  letter-spacing: .04em;
  text-transform: uppercase;
  color: var(--white);
  margin-bottom: 4px;
}
.inc-desc { font-size: .76rem; color: rgba(255,255,255,.3); }
.inc-total {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  padding: 28px 0;
  border-top: 1px solid var(--yellow);
}
.inc-price {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 4.5rem;
  color: var(--yellow);
  line-height: 1;
}
.inc-price sup { font-size: 1.6rem; vertical-align: super; }
.inc-price-label {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .58rem;
  letter-spacing: .16em;
  text-transform: uppercase;
  color: rgba(255,255,255,.28);
  margin-bottom: 4px;
  text-align: right;
}

/* ─── ACCESS STEPS ──────────────────────────────────────────── */
.step-list { border-top: 1px solid rgba(0,0,0,.1); max-width: 640px; }
.step-item {
  display: flex;
  gap: 28px;
  padding: 32px 0;
  border-bottom: 1px solid rgba(0,0,0,.1);
  align-items: flex-start;
}
.step-n {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 2.2rem;
  color: var(--yellow);
  line-height: 1;
  flex-shrink: 0;
  min-width: 32px;
}
.step-title {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: 1.05rem;
  font-weight: 700;
  letter-spacing: .05em;
  text-transform: uppercase;
  color: var(--black);
  margin-bottom: 6px;
}
.step-desc { font-size: .86rem; color: rgba(0,0,0,.5); line-height: 1.6; }

/* ─── PARA QUIÉN ES ─────────────────────────────────────────── */
.whom-ul {
  list-style: none;
  margin-top: 28px;
  display: inline-flex;
  flex-direction: column;
  gap: 16px;
  text-align: left;
}
.whom-ul li {
  display: flex;
  gap: 14px;
  font-size: .95rem;
  line-height: 1.45;
  align-items: flex-start;
  color: rgba(255,255,255,.7);
}
.ico-y { color: var(--yellow); font-weight: 700; flex-shrink: 0; }

/* ─── FAQ ───────────────────────────────────────────────────── */
.faq-list { border-top: 1px solid rgba(0,0,0,.1); }
.faq-item { border-bottom: 1px solid rgba(0,0,0,.1); }
.faq-btn {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 22px 0;
  background: none;
  border: none;
  cursor: pointer;
  font-family: 'Barlow', sans-serif;
  font-size: .98rem;
  font-weight: 600;
  color: var(--black);
  text-align: left;
  gap: 20px;
}
.faq-ico {
  width: 26px;
  height: 26px;
  border: 1.5px solid rgba(0,0,0,.2);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1rem;
  font-weight: 400;
  flex-shrink: 0;
  transition: background .2s, border-color .2s, color .2s;
}
.faq-btn[aria-expanded="true"] .faq-ico {
  background: var(--yellow);
  border-color: var(--yellow);
  color: var(--black);
}
.faq-body { max-height: 0; overflow: hidden; transition: max-height .35s ease; }
.faq-body p { font-size: .88rem; color: rgba(0,0,0,.55); line-height: 1.8; padding-bottom: 22px; padding-right: 46px; }

/* ─── PLAN CARD CTA ──────────────────────────────────────────── */
.plan-sec {
  background: var(--black);
  padding: clamp(60px,8vw,100px) 0;
  border-top: 1px solid rgba(255,255,255,.05);
  border-bottom: 1px solid rgba(255,255,255,.05);
}
.plan-wrap {
  max-width: 480px;
  margin: 0 auto;
  padding: 0 clamp(24px,5vw,48px);
}
.plan-card {
  border: 1px solid rgba(255,255,255,.1);
  outline: 1px solid rgba(244,180,0,.18);
  border-radius: 28px;
  padding: 6px;
  animation: planCardPulse 3.5s ease-in-out infinite;
}
.plan-inner {
  position: relative;
  overflow: hidden;
  border-radius: 22px;
  background: linear-gradient(160deg, rgba(255,255,255,.08) 0%, rgba(255,255,255,.02) 100%);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}
.plan-inner::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(60% 50% at 70% 0%, rgba(244,180,0,.2), transparent 70%);
  pointer-events: none;
}
.plan-body { padding: 28px; position: relative; }
.plan-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 8px;
}
.plan-label {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .65rem;
  font-weight: 700;
  letter-spacing: .2em;
  text-transform: uppercase;
  color: rgba(255,255,255,.5);
}
.plan-badge {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  border-radius: 9999px;
  background: rgba(244,180,0,.15);
  border: 1px solid rgba(244,180,0,.25);
  padding: 4px 10px;
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .6rem;
  font-weight: 700;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--yellow);
}
.plan-badge svg { flex-shrink: 0; }
.plan-price {
  font-family: 'Bebas Neue', sans-serif;
  font-size: clamp(4rem,12vw,5.5rem);
  color: var(--white);
  line-height: 1;
  letter-spacing: -.01em;
  margin-top: 4px;
}
.plan-price sup { font-size: 1.8rem; vertical-align: super; color: var(--yellow); }
.plan-period {
  font-size: .76rem;
  color: rgba(255,255,255,.35);
  margin-top: 2px;
  margin-bottom: 24px;
}
.plan-btn {
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  padding: 14px 24px;
  border-radius: 14px;
  background: linear-gradient(180deg, #F4C400 0%, #E0A800 100%);
  box-shadow: 0 10px 32px rgba(244,180,0,.22);
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .95rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--black);
  text-decoration: none;
  transition: opacity .15s, transform .2s, box-shadow .3s;
  margin-bottom: 28px;
}
.plan-btn:hover {
  opacity: .95;
  transform: translateY(-2px);
  box-shadow: 0 18px 44px rgba(244,180,0,.4);
}
.plan-divider {
  height: 1px;
  background: rgba(255,255,255,.07);
  margin-bottom: 24px;
}
.plan-features { list-style: none; display: flex; flex-direction: column; gap: 12px; }
.plan-features li {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  font-size: .9rem;
  color: rgba(255,255,255,.72);
  line-height: 1.4;
}
.plan-check {
  width: 18px;
  height: 18px;
  flex-shrink: 0;
  border-radius: 50%;
  background: rgba(244,180,0,.12);
  border: 1px solid rgba(244,180,0,.3);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 1px;
}
.plan-check svg { display: block; }

.plan-eyebrow {
  font-family: 'Barlow Condensed', sans-serif;
  font-size: .65rem;
  font-weight: 700;
  letter-spacing: .22em;
  text-transform: uppercase;
  color: rgba(255,255,255,.25);
  text-align: center;
  margin-bottom: 20px;
}
.plan-footnote {
  font-size: .7rem;
  color: rgba(255,255,255,.2);
  line-height: 1.8;
  text-align: center;
  margin-top: 20px;
  max-width: 360px;
  margin-left: auto;
  margin-right: auto;
}

/* ─── FOOTER ─────────────────────────────────────────────────── */
.foot {
  background: var(--black);
  border-top: 1px solid rgba(255,255,255,.05);
  padding: 48px 24px;
  text-align: center;
}
.foot-logo { width: 34px; height: auto; opacity: .3; margin: 0 auto 16px; display: block; }
.foot-links { display: flex; gap: 28px; justify-content: center; margin-bottom: 12px; }
.foot-links a { font-size: .66rem; color: rgba(255,255,255,.2); text-decoration: none; letter-spacing: .05em; }
.foot-links a:hover { color: var(--yellow); }
.foot-copy { font-size: .63rem; color: rgba(255,255,255,.12); letter-spacing: .04em; }

/* ─── GLASS EFFECTS ────────────────────────────────────────── */
@keyframes ctaPulse {
  0%, 100% { box-shadow: 0 16px 40px rgba(244,180,0,.28); }
  50%      { box-shadow: 0 20px 56px rgba(244,180,0,.45); }
}
@keyframes planCardPulse {
  0%, 100% { outline-color: rgba(244,180,0,.18); box-shadow: 0 0 0 rgba(244,180,0,0); }
  50%      { outline-color: rgba(244,180,0,.4);  box-shadow: 0 0 80px rgba(244,180,0,.1); }
}
.plan-btn::after,
.btn-inline::after {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(120deg, transparent 30%, rgba(255,255,255,.55) 50%, transparent 70%);
  transform: translateX(-100%);
  transition: transform .7s ease;
  pointer-events: none;
}
.plan-btn:hover::after,
.btn-inline:hover::after {
  transform: translateX(100%);
}

.reveal { opacity: 0; transform: translateY(16px); transition: opacity .6s ease, transform .6s ease; }
.reveal.in { opacity: 1; transform: none; }

@media (max-width: 768px) {
  .proof-grid       { grid-template-columns: repeat(2, 1fr); }
  .method-item      { grid-template-columns: 60px 1fr; }
  .method-n         { font-size: 2.8rem; }
  .method-body      { padding-left: 20px; }
}
@media (max-width: 480px) {
  .method-item      { grid-template-columns: 1fr; }
  .method-n         { font-size: 2.2rem; padding-right: 0; border-right: none; padding-bottom: 12px; border-bottom: 1px solid rgba(0,0,0,.1); margin-bottom: 16px; }
  .method-body      { padding-left: 0; }
}
</style>

<!-- ── HERO VSL (página cerrada hasta 2:40) ─────────────────── -->
<section class="hero">
  <div class="hero-inner">
    <div class="hero-text">
      <h1 class="hero-headline">
        Un plan práctico de entrenamientos para porteros con foco en
        <span class="yl">explosión, fuerza y velocidad</span>,
        que pueden hacerse en casa o en el gimnasio de tu barrio.
      </h1>
      <div class="hero-vsl">
        <vturb-smartplayer id="vid-6a18a8b89450495a8b528dfa" style="display: block; margin: 0 auto; width: 100%; max-width: 400px;"></vturb-smartplayer>
      </div>
      <a href="https://pay.hotmart.com/J106225942D" class="hero-cta is-hidden" id="hero-cta">
        Acceder al Desafío
      </a>
    </div>
  </div>
</section>

<!-- ── CRONOGRAMA DE LA SEMANA ───────────────────────────────── -->
<section class="schedule-sec">
  <div class="schedule-intro reveal">
    <div class="label">Cronograma de la semana</div>
    <h3>5 días. 5 entrenamientos.<br>Todo lo que decide en el arco.</h3>
  </div>
  <div class="schedule-grid reveal" style="transition-delay:.08s">
    <div class="schedule-card">
      <div class="schedule-n">01</div>
      <div class="schedule-day">Lunes</div>
      <div class="schedule-title">Fuerza estructural</div>
      <div class="schedule-desc">Base de fuerza para todo el cuerpo: cuádriceps, posterior, glúteo, core. Sostén para todo lo que viene.</div>
    </div>
    <div class="schedule-card">
      <div class="schedule-n">02</div>
      <div class="schedule-day">Martes</div>
      <div class="schedule-title">Empujes y complementos</div>
      <div class="schedule-desc">Empujar vertical y horizontal. Movimiento de golpe y disputa de pelota alta. Lanzamientos, potencia y explosión.</div>
    </div>
    <div class="schedule-card">
      <div class="schedule-n">03</div>
      <div class="schedule-day">Miércoles</div>
      <div class="schedule-title">Potencia</div>
      <div class="schedule-desc">Explosión pura: pliometría, saltos, arranques cortos.</div>
    </div>
    <div class="schedule-card">
      <div class="schedule-n">04</div>
      <div class="schedule-day">Jueves</div>
      <div class="schedule-title">Tirones y complementos</div>
      <div class="schedule-desc">Tirar vertical y horizontal. Disputa aérea, estabilidad en el salto, coordinación entre brazo y pierna. Postural.</div>
    </div>
    <div class="schedule-card">
      <div class="schedule-n">05</div>
      <div class="schedule-day">Viernes</div>
      <div class="schedule-title">Preventivos y aceleración</div>
      <div class="schedule-desc">Cero lesiones + velocidad de movimiento, fuerza coordinativa. Cierra la semana listo para el partido.</div>
    </div>
  </div>
</section>

<!-- ── COUNTDOWN ─────────────────────────────────────────────── -->
<section class="s-dark">
  <div class="wrap">
    <div class="cd-block reveal">
      <div class="label">Próxima clase</div>
      <div class="cd-time" id="gde-cd">--&nbsp;·&nbsp;--&nbsp;·&nbsp;--&nbsp;·&nbsp;--</div>
      <div class="cd-labels">Días · Horas · Min · Seg</div>
      <div class="cd-date">comienza <span id="gde-date">lunes</span></div>
      <div class="cd-bar"><div class="cd-fill"></div></div>
      <div class="cd-slots"><strong>18 cupos</strong> restantes de 70</div>
      <a href="https://pay.hotmart.com/J106225942D" class="btn-inline cd-cta">
        Asegurar mi cupo · $10 USD →
      </a>
      <p class="cd-foot">Pago único · acceso inmediato</p>
    </div>
  </div>
</section>

<!-- ── EL PROBLEMA ─────────────────────────────────────────────── -->
<section class="s-light">
  <div class="wrap">
    <div class="reveal">
      <div class="label">El problema</div>
      <h2 class="display">Entrenas.<br>Te dedicas.<br>Pero a la hora del partido…</h2>
    </div>
    <ul class="pain-list reveal" style="transition-delay:.08s">
      <li class="pain-item">
        <div class="pain-x">✗</div>
        <span>Piernas que no responden en el momento decisivo</span>
      </li>
      <li class="pain-item">
        <div class="pain-x">✗</div>
        <span>Te caes y tardas en levantarte para la próxima jugada</span>
      </li>
      <li class="pain-item">
        <div class="pain-x">✗</div>
        <span>Pierdes la disputa aérea en el segundo tiempo</span>
      </li>
      <li class="pain-item">
        <div class="pain-x">✗</div>
        <span>Entrenas en el gimnasio pero no mejoras en el arco</span>
      </li>
      <li class="pain-item">
        <div class="pain-x">✗</div>
        <span>Te meten gol que sabías que era para atajar</span>
      </li>
    </ul>
    <div class="pivot-quote reveal" style="transition-delay:.14s">
      No faltó entrenamiento. No faltó esfuerzo.<br>
      <strong>Faltó el tipo correcto de entrenamiento para portero.</strong>
    </div>
  </div>
</section>

<!-- ── PARA QUIÉN ES ──────────────────────────────────────────── -->
<section class="s-dark">
  <div class="wrap">
    <div class="reveal" style="text-align:center;max-width:560px;margin:0 auto">
      <div class="label" style="justify-content:center">Para quién es</div>
      <h2 class="display" style="margin-bottom:32px">Este desafío es <span class="yl">tuyo</span> si…</h2>
      <ul class="whom-ul">
        <li><span class="ico-y">✓</span> Eres portero amateur, universitario, de base o aspirante</li>
        <li><span class="ico-y">✓</span> Entrenas pero no ves mejora dentro del campo</li>
        <li><span class="ico-y">✓</span> Quieres entrenamiento físico específico para portero — no genérico</li>
        <li><span class="ico-y">✓</span> Puedes separar 40–60 min al día durante 5 días</li>
      </ul>
    </div>
  </div>
</section>

<!-- ── RESULTADOS REALES ─────────────────────────────────────── -->
<section class="s-dark">
  <div class="wrap-wide">
    <div class="reveal">
      <div class="label">Resultados reales</div>
      <h2 class="display">Lo que cambia cuando<br>el portero entrena <span class="yl">bien</span></h2>
    </div>
    <div class="proof-clip reveal" style="transition-delay:.1s">
    <div class="proof-carousel-outer">
      <button class="proof-nav proof-nav-prev" aria-label="Anterior">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
      </button>
      <div class="proof-track">
        <div class="proof-slide" data-index="0">
          <img src="/images/carrossel-1.webp" alt="Fabíola Stelzer" width="3600" height="3600" loading="lazy" decoding="async" />
          <div class="proof-slide-overlay"></div>
          <div class="proof-active-badge">Destacado</div>
          <div class="proof-caption">
            <div class="proof-role-pill">Portera Amateur</div>
            <div class="proof-slide-name">Fabíola Stelzer</div>
          </div>
        </div>
        <div class="proof-slide" data-index="1">
          <img src="/images/carrossel-2.webp" alt="Rafael S." width="3600" height="3600" loading="lazy" decoding="async" />
          <div class="proof-slide-overlay"></div>
          <div class="proof-active-badge">Destacado</div>
          <div class="proof-caption">
            <div class="proof-role-pill">Portero Profesional</div>
            <div class="proof-slide-name">Rafael S.</div>
          </div>
        </div>
        <div class="proof-slide" data-index="2">
          <img src="/images/carrossel-3.webp" alt="Diego A." width="1600" height="1600" loading="lazy" decoding="async" />
          <div class="proof-slide-overlay"></div>
          <div class="proof-active-badge">Destacado</div>
          <div class="proof-caption">
            <div class="proof-role-pill">Portero de Base</div>
            <div class="proof-slide-name">Diego A.</div>
          </div>
        </div>
        <div class="proof-slide" data-index="3">
          <img src="/images/carrossel-4.webp" alt="Thiago R." width="739" height="1600" loading="lazy" decoding="async" />
          <div class="proof-slide-overlay"></div>
          <div class="proof-active-badge">Destacado</div>
          <div class="proof-caption">
            <div class="proof-role-pill">Portero Amateur</div>
            <div class="proof-slide-name">Thiago R.</div>
          </div>
        </div>
        <div class="proof-slide" data-index="4">
          <img src="/images/carrossel-5.webp" alt="Matheus F." width="739" height="1600" loading="lazy" decoding="async" />
          <div class="proof-slide-overlay"></div>
          <div class="proof-active-badge">Destacado</div>
          <div class="proof-caption">
            <div class="proof-role-pill">Portero Aspirante</div>
            <div class="proof-slide-name">Matheus F.</div>
          </div>
        </div>
        <div class="proof-slide" data-index="5">
          <img src="/images/carrossel-6.webp" alt="Bruno C." width="720" height="1600" loading="lazy" decoding="async" />
          <div class="proof-slide-overlay"></div>
          <div class="proof-active-badge">Destacado</div>
          <div class="proof-caption">
            <div class="proof-role-pill">Portero Universitario</div>
            <div class="proof-slide-name">Bruno C.</div>
          </div>
        </div>
      </div>
      <button class="proof-nav proof-nav-next" aria-label="Siguiente">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
      </button>
    </div>
    </div>
  </div>
</section>

<!-- ── EL MÉTODO ──────────────────────────────────────────────── -->
<section class="s-light">
  <div class="wrap">
    <div class="reveal">
      <div class="label">El método</div>
      <h2 class="display" style="color:var(--black)">4 entregas.<br><span style="color:var(--black);opacity:.3">4 cambios en tu juego.</span></h2>
    </div>
    <div class="method-list reveal" style="transition-delay:.08s">
      <div class="method-item">
        <div class="method-n">01</div>
        <div class="method-body">
          <div class="method-title">Entrenamiento específico</div>
          <ul class="method-ul">
            <li>Explosión, fuerza y velocidad</li>
            <li>Sin pelota, sin técnica — solo lo que decide en el partido</li>
          </ul>
          <div class="method-result">Piernas que responden en el momento justo. Te levantas rápido, explotas al ángulo, disputa aérea fuerte hasta el final del partido.</div>
        </div>
      </div>
      <div class="method-item">
        <div class="method-n">02</div>
        <div class="method-body">
          <div class="method-title">Acompañamiento durante la semana</div>
          <ul class="method-ul">
            <li>Entrenadores certificados activos en el grupo</li>
            <li>Mandas video, ellos corrigen — sin horario fijo</li>
          </ul>
          <div class="method-result">Confianza real: entras a la cancha sabiendo que el cuerpo responde. Dejas de dudar en el momento de decidir.</div>
        </div>
      </div>
      <div class="method-item">
        <div class="method-n">03</div>
        <div class="method-body">
          <div class="method-title">Menú nutricional de la semana</div>
          <ul class="method-ul">
            <li>Plan alimenticio simple y práctico</li>
            <li>Optimizado para los 5 días de entrenamiento</li>
          </ul>
          <div class="method-result">Energía para entrenar al ritmo correcto, sin llegar agotado al final de la semana.</div>
        </div>
      </div>
      <div class="method-item">
        <div class="method-n">04</div>
        <div class="method-body">
          <div class="method-title">Ciclo + material permanente</div>
          <ul class="method-ul">
            <li>Entrenamiento liberado en la víspera, gimnasio o casa</li>
            <li>Material disponible para siempre</li>
          </ul>
          <div class="method-result">Nunca más entrenas a ciegas. Sabes qué hacer, cómo y por qué — para siempre.</div>
        </div>
      </div>
    </div>
    <div class="method-bonus reveal" style="transition-delay:.16s">
      <strong>+ Bono:</strong> 5 días con porteros serios y comprometidos. El ambiente cambia cómo entrenas solo.
    </div>
  </div>
</section>

<!-- ── LO QUE RECIBES ────────────────────────────────────────── -->
<section class="s-dark" style="border-top:1px solid rgba(255,255,255,.05)">
  <div class="wrap">
    <div class="reveal">
      <div class="label">Lo que recibes</div>
      <h2 class="display">Todo por <span class="yl">$10 USD</span></h2>
    </div>
    <div class="inc-table reveal" style="transition-delay:.1s">
      <div class="inc-row">
        <div>
          <div class="inc-name">Acompañamiento activo en el grupo durante la semana</div>
          <div class="inc-desc">Entrenadores certificados corrigen tu video · Q&A diario · sin horario fijo</div>
        </div>
      </div>
      <div class="inc-row">
        <div>
          <div class="inc-name">5 entrenamientos físicos completos</div>
          <div class="inc-desc">Versión gimnasio + versión casa · de lunes a viernes</div>
        </div>
      </div>
      <div class="inc-row">
        <div>
          <div class="inc-name">Menú nutricional de la semana</div>
          <div class="inc-desc">Plan alimenticio específico para los 5 días</div>
        </div>
      </div>
      <div class="inc-row">
        <div>
          <div class="inc-name">Material disponible para siempre</div>
          <div class="inc-desc">Rehaz los entrenamientos cuando quieras · acceso permanente al contenido</div>
        </div>
      </div>
      <div class="inc-total">
        <div>
          <div class="inc-price-label">Pagas (USD)</div>
          <div class="inc-price"><sup>$</sup>10</div>
        </div>
      </div>
      <div class="btn-inline-wrap reveal" style="transition-delay:.12s">
        <a href="https://pay.hotmart.com/J106225942D" class="btn-inline">
          Asegurar mi cupo ahora →
        </a>
      </div>
    </div>
  </div>
</section>

<!-- ── CÓMO RECIBO MI ACCESO ──────────────────────────────────── -->
<section class="s-light">
  <div class="wrap">
    <div class="reveal">
      <div class="label">Cómo funciona</div>
      <h2 class="display" style="color:var(--black)">¿Cómo recibo<br>mi acceso?</h2>
    </div>
    <div class="step-list reveal" style="transition-delay:.1s">
      <div class="step-item">
        <div class="step-n">1</div>
        <div>
          <div class="step-title">Haz tu inscripción</div>
          <div class="step-desc">Paga $10 USD · pago único · procesado al instante</div>
        </div>
      </div>
      <div class="step-item">
        <div class="step-n">2</div>
        <div>
          <div class="step-title">Entras al grupo de acompañamiento</div>
          <div class="step-desc">Los entrenadores ya están ahí · entrenas en tu horario · soporte directo por video</div>
        </div>
      </div>
      <div class="step-item">
        <div class="step-n">3</div>
        <div>
          <div class="step-title">Comienza el próximo lunes</div>
          <div class="step-desc">Entrenamiento liberado el domingo por la noche · acceso permanente al material</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── FAQ ────────────────────────────────────────────────────── -->
<section class="s-light">
  <div class="wrap">
    <div class="reveal">
      <div class="label">Dudas</div>
      <h2 class="display" style="color:var(--black)">Preguntas<br>frecuentes</h2>
    </div>
    <div class="faq-list reveal" style="transition-delay:.1s">
      <div class="faq-item">
        <button class="faq-btn" aria-expanded="false">
          ¿Necesito tener gimnasio para hacerlo?
          <div class="faq-ico">+</div>
        </button>
        <div class="faq-body"><p>No. Cada entrenamiento viene en dos versiones: gimnasio y casa. Tú eliges.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-btn" aria-expanded="false">
          ¿Funciona para portero amateur?
          <div class="faq-ico">+</div>
        </button>
        <div class="faq-body"><p>Sí. El desafío fue creado específicamente para porteros amateurs, universitarios, de base y aspirantes.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-btn" aria-expanded="false">
          ¿Hay horario fijo? ¿Cuánto tiempo necesito separar al día?
          <div class="faq-ico">+</div>
        </button>
        <div class="faq-body"><p>No hay horario fijo. El entrenamiento se libera la noche anterior y lo haces cuando puedas, a tu ritmo. Cada entrenamiento dura entre 40 y 60 minutos.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-btn" aria-expanded="false">
          ¿Y si pierdo un día o quiero rehacerlo después?
          <div class="faq-ico">+</div>
        </button>
        <div class="faq-body"><p>Sin problema. El material queda disponible para siempre — retomas al día siguiente, o rehaces todo el desafío cuando quieras, a tu propio ritmo.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-btn" aria-expanded="false">
          ¿Cómo recibo mi acceso?
          <div class="faq-ico">+</div>
        </button>
        <div class="faq-body"><p>Inmediatamente después del pago serás dirigido al grupo de acompañamiento. Sin espera, sin email por llegar.</p></div>
      </div>
    </div>
  </div>
</section>

<!-- ── CTA FINAL ──────────────────────────────────────────────── -->
<section class="plan-sec" style="border-bottom:none">
  <div class="plan-wrap">
    <p class="plan-eyebrow reveal">Próxima clase · <span id="gde-date-cta">lunes</span></p>
    <div class="plan-card reveal" style="transition-delay:.06s">
      <div class="plan-inner">
        <div class="plan-body">

          <div class="plan-header">
            <div class="plan-label">Plan único</div>
            <div class="plan-badge">
              <svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
              Más popular
            </div>
          </div>

          <div class="plan-price"><sup>$</sup>10</div>
          <div class="plan-period">
            pago único &nbsp;·&nbsp; USD
          </div>

          <a href="https://pay.hotmart.com/J106225942D" class="plan-btn">
            Asegurar mi cupo ahora
          </a>

          <div class="plan-divider"></div>

          <ul class="plan-features">
            <li>
              <span class="plan-check">
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#F4B400" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
              </span>
              5 entrenamientos físicos específicos para portero (gimnasio + casa)
            </li>
            <li>
              <span class="plan-check">
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#F4B400" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
              </span>
              Acompañamiento de los entrenadores durante toda la semana
            </li>
            <li>
              <span class="plan-check">
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#F4B400" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
              </span>
              Corrección por video + Q&amp;A diario durante los 5 días
            </li>
            <li>
              <span class="plan-check">
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#F4B400" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
              </span>
              Menú nutricional específico para los 5 días
            </li>
            <li>
              <span class="plan-check">
                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#F4B400" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
              </span>
              Material disponible para siempre — rehaz cuando quieras
            </li>
          </ul>

        </div>
      </div>
    </div>
    <p class="plan-footnote reveal" style="transition-delay:.1s">El precio es intencional. Este desafío es la puerta de entrada — queremos que pruebes el método antes de cualquier decisión mayor.</p>
  </div>
</section>

<!-- ── FOOTER ─────────────────────────────────────────────────── -->
<footer class="foot">
  <img src="/images/logo-gde.webp" alt="Goleiro de Elite" class="foot-logo" width="34" height="34" loading="lazy" decoding="async" />
  <div class="foot-links">
    <a href="#">Términos de uso</a>
    <a href="#">Política de privacidad</a>
  </div>
  <p class="foot-copy">© 2026 Goleiro de Elite. Todos los derechos reservados.</p>
</footer>

<script>
  // ── Locked state: página cerrada hasta 2:40 del VSL ────────
  document.body.classList.add('is-locked');
  (function loadVSL() {
    const s = document.createElement('script');
    s.src = 'https://scripts.converteai.net/54614f94-0915-47ec-b3bb-c6614fdad9d2/players/6a18a8b89450495a8b528dfa/v4/player.js';
    s.async = true;
    document.head.appendChild(s);
  })();

  // ── VSL gate persistente: descuenta tiempo ya visto ────────
  function initVslGate({ storageKey, targetSeconds, playerId, onUnlock }) {
    const EXPIRY_MS = 30 * 24 * 60 * 60 * 1000;
    let unlocked = false;

    function readState() {
      try {
        const raw = localStorage.getItem(storageKey);
        if (!raw) return 0;
        const s = JSON.parse(raw);
        if (s.playerId !== playerId) return 0;
        if (Date.now() - s.savedAt > EXPIRY_MS) return 0;
        return Number(s.watched) || 0;
      } catch { return 0; }
    }

    function saveState(watched) {
      try {
        localStorage.setItem(storageKey, JSON.stringify({
          watched, savedAt: Date.now(), playerId
        }));
      } catch {}
    }

    let maxWatched = readState();

    function tryUnlock() {
      if (unlocked) return;
      unlocked = true;
      saveState(targetSeconds);
      onUnlock();
    }

    if (maxWatched >= targetSeconds) { tryUnlock(); return; }

    const remainingMs = (targetSeconds - maxWatched) * 1000;
    const fallbackTimer = setTimeout(tryUnlock, remainingMs);

    const pollInterval = setInterval(() => {
      if (unlocked) { clearInterval(pollInterval); return; }
      const el = document.getElementById(playerId);
      if (!el) return;
      let t = Number(el.currentTime);
      if (!isFinite(t) || t < 0) {
        const innerVideo = el.querySelector && el.querySelector('video');
        if (innerVideo) t = innerVideo.currentTime;
      }
      if (!isFinite(t) || t < 0) return;
      if (t > maxWatched) {
        maxWatched = Math.floor(t);
        saveState(maxWatched);
        if (maxWatched >= targetSeconds) {
          clearTimeout(fallbackTimer);
          tryUnlock();
        }
      }
    }, 1000);
  }

  initVslGate({
    storageKey: 'vsl_landing_es',
    targetSeconds: 160,
    playerId: 'vid-6a18a8b89450495a8b528dfa',
    onUnlock: () => {
      document.body.classList.remove('is-locked');
      const cta = document.getElementById('hero-cta');
      if (cta) cta.classList.remove('is-hidden');
    }
  });

  function nextMonday() {
    const fixed = new Date('2026-06-01T03:00:00Z');
    if (Date.now() < fixed.getTime()) return fixed;
    const brt = new Date(new Date().toLocaleString('en-US', { timeZone: 'America/Sao_Paulo' }));
    const d = brt.getDay();
    const days = (8 - d) % 7 || 7;
    const m = new Date(brt);
    m.setDate(brt.getDate() + days);
    m.setHours(0, 0, 0, 0);
    return m;
  }
  function pad(n) { return String(n).padStart(2, '0'); }
  function tick() {
    const now = new Date(new Date().toLocaleString('en-US', { timeZone: 'America/Sao_Paulo' }));
    const target = nextMonday();
    const diff = target - now;
    if (diff <= 0) { tick(); return; }
    const dd = Math.floor(diff / 86400000);
    const h  = Math.floor((diff % 86400000) / 3600000);
    const m  = Math.floor((diff % 3600000)  / 60000);
    const s  = Math.floor((diff % 60000)    / 1000);
    const str = `${pad(dd)} · ${pad(h)} · ${pad(m)} · ${pad(s)}`;
    const el = document.getElementById('gde-cd');
    if (el) el.textContent = str;
    const dateStr = target.toLocaleDateString('es', {
      weekday: 'long', day: 'numeric', month: 'long', timeZone: 'America/Sao_Paulo'
    });
    ['gde-date', 'gde-date-cta'].forEach(id => {
      const el = document.getElementById(id);
      if (el) el.textContent = dateStr;
    });
  }
  tick();
  setInterval(tick, 1000);

  document.querySelectorAll('.faq-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const open = btn.getAttribute('aria-expanded') === 'true';
      document.querySelectorAll('.faq-btn').forEach(b => {
        b.setAttribute('aria-expanded', 'false');
        b.nextElementSibling.style.maxHeight = null;
        b.querySelector('.faq-ico').textContent = '+';
      });
      if (!open) {
        btn.setAttribute('aria-expanded', 'true');
        btn.nextElementSibling.style.maxHeight = btn.nextElementSibling.scrollHeight + 'px';
        btn.querySelector('.faq-ico').textContent = '−';
      }
    });
  });

  // ── Proof carousel ──────────────────────────────────────────
  (function() {
    const slides = Array.from(document.querySelectorAll('.proof-slide'));
    const total = slides.length;
    let current = 2;
    const INTERVAL = 3500;

    function update() {
      slides.forEach((slide, i) => {
        let rel = i - current;
        if (rel > total / 2)  rel -= total;
        if (rel < -total / 2) rel += total;

        slide.classList.remove('is-active');
        let tx, scale, rotY, opacity, brightness, zIndex, shadow;

        if (rel === 0) {
          tx = 0; scale = 1; rotY = 0; opacity = 1; brightness = 1; zIndex = 10;
          shadow = '0 20px 60px rgba(244,180,0,.22)';
          slide.classList.add('is-active');
        } else if (Math.abs(rel) === 1) {
          tx = rel * 200; scale = 0.87; rotY = rel > 0 ? -15 : 15;
          opacity = 0.55; brightness = 0.7; zIndex = 5; shadow = 'none';
        } else if (Math.abs(rel) === 2) {
          tx = rel * 340; scale = 0.76; rotY = rel > 0 ? -25 : 25;
          opacity = 0.3; brightness = 0.5; zIndex = 2; shadow = 'none';
        } else {
          tx = 0; scale = 0.65; rotY = 0;
          opacity = 0; brightness = 0.4; zIndex = 1; shadow = 'none';
        }

        slide.style.transform = `translateX(${tx}px) scale(${scale}) rotateY(${rotY}deg)`;
        slide.style.opacity = opacity;
        slide.style.filter = `brightness(${brightness})`;
        slide.style.zIndex = zIndex;
        slide.style.boxShadow = shadow;
      });
    }

    function next() { current = (current + 1) % total; update(); }
    function prev() { current = (current - 1 + total) % total; update(); }

    let timer = setInterval(next, INTERVAL);
    const outer = document.querySelector('.proof-clip');
    outer.addEventListener('mouseenter', () => clearInterval(timer));
    outer.addEventListener('mouseleave', () => { timer = setInterval(next, INTERVAL); });

    document.querySelector('.proof-nav-prev').addEventListener('click', () => { prev(); clearInterval(timer); timer = setInterval(next, INTERVAL); });
    document.querySelector('.proof-nav-next').addEventListener('click', () => { next(); clearInterval(timer); timer = setInterval(next, INTERVAL); });
    slides.forEach((slide, i) => {
      slide.addEventListener('click', () => { current = i; update(); clearInterval(timer); timer = setInterval(next, INTERVAL); });
    });

    update();
  })();

  const ro = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) { e.target.classList.add('in'); ro.unobserve(e.target); }
    });
  }, { threshold: 0.06 });
  document.querySelectorAll('.reveal').forEach(el => ro.observe(el));
</script>
</Base>

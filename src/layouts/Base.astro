---
interface Props {
  title?: string;
}
const { title = "Método Goleiro 2.0 | Preparação Física para Goleiros" } = Astro.props;
---
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content="+1.200 goleiros já treinaram com o Método Goleiro 2.0. Musculação específica para goleiros — planos individualizados, 100% online, sem mensalidade." />
  <meta property="og:title" content="Método Goleiro 2.0 | Musculação Específica para Goleiros" />
  <meta property="og:description" content="+1.200 goleiros já treinaram com o Método Goleiro 2.0. Musculação específica para goleiros, 100% online, sem mensalidade." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://metodo.goleirodeelite.com/" />
  <meta property="og:image" content="https://metodo.goleirodeelite.com/images/hero-antes-depois.webp" />
  <meta name="twitter:card" content="summary_large_image" />
  <!-- Preload imagem hero (LCP) -->
  <link rel="preload" as="image" href="/images/hero-antes-depois.webp" fetchpriority="high" />
  <!-- Google Tag Manager -->
  <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],j=d.createElement(s);j.async=true;j.src="https://server.goleirodeelite.com/9smvimemi.js?"+i;f.parentNode.insertBefore(j,f);})(window,document,'script','dataLayer','5wlfyfq=AAlYKj07QCMrWVMrWyBVVR9QV0NYUAkdVAoKAQwfHxwNCAABAAIIXQoCCA%3D%3D');</script>
  <!-- End Google Tag Manager -->
  <!-- Fontes não-bloqueantes -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@400;600;700;800&family=JetBrains+Mono:wght@400;700&display=swap" onload="this.onload=null;this.rel='stylesheet'" />
  <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@400;600;700;800&family=JetBrains+Mono:wght@400;700&display=swap" /></noscript>
  <style is:global>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg:       #0A0A0A;
      --card:     #161616;
      --neon:     #DFFF00;
      --neon-dim: rgba(223,255,0,.07);
      --white:    #F5F5F5;
      --gray:     #888;
      --muted:    #666666;
      --border:   #222;
      --selected: #141a00;
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--bg);
      color: var(--white);
      font-family: 'Barlow Condensed', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* grain */
    body::after {
      content: '';
      position: fixed;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
      opacity: .05;
      pointer-events: none;
      z-index: 9999;
    }

    .page {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 0 16px 100px;
    }

    .hr {
      width: 100%;
      max-width: 680px;
      height: 1px;
      background: linear-gradient(to right, transparent, var(--muted), transparent);
      margin: 8px 0;
    }

    .section-tag {
      font-family: 'JetBrains Mono', monospace;
      font-size: .75rem;
      font-weight: 700;
      color: var(--neon);
      letter-spacing: .2em;
      text-transform: uppercase;
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px;
    }
    .section-tag::before { content: '//'; color: var(--muted); }

    .reveal {
      opacity: 0;
      transform: translateY(24px);
      transition: opacity .6s ease, transform .6s ease;
    }
    .reveal.in { opacity: 1; transform: translateY(0); }

    :focus-visible {
      outline: 2px solid var(--neon);
      outline-offset: 3px;
    }

    @keyframes shake {
      0%,100% { transform: translateX(0); }
      20%      { transform: translateX(-8px); }
      40%      { transform: translateX(8px); }
      60%      { transform: translateX(-5px); }
      80%      { transform: translateX(5px); }
    }
    @keyframes blink {
      0%,100% { opacity: 1; }
      50%      { opacity: .2; }
    }
    @keyframes pulse {
      0%   { box-shadow: 0 0 0 0 rgba(223,255,0,.5), 0 8px 40px rgba(223,255,0,.2); }
      55%  { box-shadow: 0 0 0 16px rgba(223,255,0,0), 0 8px 40px rgba(223,255,0,.2); }
      100% { box-shadow: 0 0 0 0 rgba(223,255,0,0), 0 8px 40px rgba(223,255,0,.2); }
    }
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(8px); }
      to   { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <!-- Google Tag Manager (noscript) -->
  <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-PB46F2V8"
  height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
  <!-- End Google Tag Manager (noscript) -->

  <slot />
</body>
</html>
